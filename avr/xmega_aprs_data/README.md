Example of generating APRS AX.25 packet on xmega device using DAC and DMA transfers.


I recommend Ubuntu and avr-gcc with make.
Execute the following to compile the project:

make clean && make


To flash the project to your chip execute the following:

make flash
